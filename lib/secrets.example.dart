/// Template for `lib/secrets.dart` — copy this to `lib/secrets.dart` and fill
/// in the real values. `lib/secrets.dart` is git-ignored.
class Secrets {
  /// Google Gemini API key created at https://aistudio.google.com/app/apikey
  static const geminiApiKey = 'YOUR_GEMINI_API_KEY_HERE';
}
